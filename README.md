## Kuberentes random password
Generate random a password using a Kubernetes Job. So you won't need to encrypt a per environment password. It's random everytime.

You can use `job.yaml` as a starting point.
